<%= form_with(model: salle) do |form| %>
  <% if salle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(salle.errors.count, "error") %> prohibited this salle from being saved:</h2>

      <ul>
        <% salle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nomsalle %>
    <%= form.text_field :nomsalle %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
